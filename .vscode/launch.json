{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "🐍 Python: Attach to Docker (FastAPI)",
      "type": "debugpy",
      "request": "attach",
      "connect": {
        "host": "localhost",
        "port": 5725
      },
      "pathMappings": [
        {
          "localRoot": "${workspaceFolder}/backend",
          "remoteRoot": "/backend"
        }
      ]
    }
  ]
}