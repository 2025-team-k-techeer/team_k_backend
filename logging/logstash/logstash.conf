input {
  beats {
    port => 5044
  }
}

filter {
  # Example: drop logs before a specific time
  if [@timestamp] < "2025-07-17T10:20:00.000Z" {
    drop { }
  }

  # Example: grok to extract log level (uncomment if needed)
  # grok {
  #   match => { "message" => "\[%{LOGLEVEL:loglevel}\] %{GREEDYDATA:message}" }
  # }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "app-logs-%{+YYYY.MM.dd}"
    user => "elastic"
    password => "password"
  }
}
